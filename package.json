{
  "scripts": {
    "check": "node --check game.js",
    "firebase:version": "firebase --version",
    "deploy": "firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.13.0"
  }
}
